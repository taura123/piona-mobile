PORT=8080
HOST=0.0.0.0

# PostgreSQL
DATABASE_URL="postgresql://piona:piona_dev_password@localhost:5432/piona?schema=public"

# Auth
JWT_SECRET="replace_me_with_a_long_random_string"

# Dev-only default user (created if not exists)
DEV_DEFAULT_USERNAME="admin"
DEV_DEFAULT_PASSWORD="admin123"
DEV_DEFAULT_ROLE="Admin"

